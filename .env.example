# Database - Para desarrollo local (sin Docker)
# Si usas docker-compose, esta variable se sobrescribe automáticamente
MONGODB_URI=mongodb://localhost:27017/kanban-board?replicaSet=rs0
DATABASE_URL=${MONGODB_URI}

# Nota: Docker Compose usa:
# mongodb://root:example@mongo:27017/kanban-board?authSource=admin&replicaSet=rs0
# donde "mongo" es el nombre del servicio de MongoDB DENTRO de Docker
# replicaSet=rs0 es requerido por Prisma para usar transacciones

# Backend
PORT=3000
JWT_SECRET=super-secret-change-me
N8N_WEBHOOK_URL=http://n8n:5678/webhook/kanban-export
# Opcional: define la ruta del webhook si usas un nombre distinto
# N8N_WEBHOOK_PATH=kanban-export
# Opcional: base alternativa (por ejemplo para ejecución local fuera de Docker)
# N8N_WEBHOOK_BASE_URL=http://localhost:5678

# Frontend
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000
EXPORT_NOTIFICATION_EMAIL=team@localhost.test
